// This component is no longer used and can be deleted.
// The content has been moved to Bonuses.tsx and Faq.tsx.
// We will leave it for now to avoid breaking imports, but it's deprecated.
export function CourseContent() {
    return null;
}
