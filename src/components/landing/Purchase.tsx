// This component is no longer used and has been replaced by Urgency.tsx and FinalCta.tsx.
// We will leave it for now to avoid breaking imports, but it's deprecated.
export function Purchase() {
    return null;
}